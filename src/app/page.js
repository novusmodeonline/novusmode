import { HomepageClient } from "@/components";

export const metadata = {
  title: "Home | NovusMode",
  alternates: { canonical: "/" },
};

export default function Home() {
  return <HomepageClient />;
}
